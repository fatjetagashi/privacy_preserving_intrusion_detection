# privacy_preserving_intrusion_detection

## Data Files

CSV and parquet data files under `data/` are tracked with Git LFS. After
cloning on a new machine, run `git lfs install` once if needed and then
`git lfs pull` to fetch the actual dataset contents. Auxiliary files under
`data/` such as JSON metadata and Spark status/checksum files are committed
normally.
